name: BC CI/CD Pipeline

on:
  push:
    branches:
      - main


jobs:
  build:
    name: Build AL Extension
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup PowerShell
        shell: pwsh
        run: |
          Write-Host "Starting Build Process..."

      - name: Install BcContainerHelper
        shell: pwsh
        run: |
          Install-Module BcContainerHelper -Force -Scope CurrentUser
        
      # - name: Check if BC is available
      #   shell: pwsh
      #   run: |
      #     Get-NAVServerInstance

      - name: Compile AL project
        shell: pwsh
        run: |
          # Path where your AL project is.....
          $projectPath = "C:\Users\Levi\Desktop\Attachment\SD Seminar"

          # Output folder for app
          $outputPath = "$projectPath\output"

          New-Item -ItemType Directory -Force -Path $outputPath

          # share the project file
          New-BcContainerFileShare `
            -containerName bcserver `
            -hostFolder "C:\Users\Levi\Desktop\Attachment\SD Seminar"

          # Build app using AL compiler (inside container context)
          Compile-AppInBcContainer `
            -containerName "bcserver" `
            -appProjectFolder $projectPath `
            -appOutputFolder $outputPath

      - name: Upload artifact (.app file)
        uses: actions/upload-artifact@v4
        with:
          name: bc-extension
          path: output/*.app